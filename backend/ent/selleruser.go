// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/entregamais/platform/backend/ent/seller"
	"github.com/entregamais/platform/backend/ent/selleruser"
	"github.com/entregamais/platform/backend/ent/user"
)

// SellerUser is the model entity for the SellerUser schema.
type SellerUser struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Role holds the value of the "role" field.
	Role string `json:"role,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SellerUserQuery when eager-loading is set.
	Edges             SellerUserEdges `json:"edges"`
	seller_users      *string
	user_seller_links *string
	selectValues      sql.SelectValues
}

// SellerUserEdges holds the relations/edges for other nodes in the graph.
type SellerUserEdges struct {
	// Seller holds the value of the seller edge.
	Seller *Seller `json:"seller,omitempty"`
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// SellerOrErr returns the Seller value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SellerUserEdges) SellerOrErr() (*Seller, error) {
	if e.Seller != nil {
		return e.Seller, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: seller.Label}
	}
	return nil, &NotLoadedError{edge: "seller"}
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SellerUserEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SellerUser) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case selleruser.FieldID, selleruser.FieldRole:
			values[i] = new(sql.NullString)
		case selleruser.FieldCreatedAt, selleruser.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case selleruser.ForeignKeys[0]: // seller_users
			values[i] = new(sql.NullString)
		case selleruser.ForeignKeys[1]: // user_seller_links
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SellerUser fields.
func (_m *SellerUser) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case selleruser.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case selleruser.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case selleruser.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case selleruser.FieldRole:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field role", values[i])
			} else if value.Valid {
				_m.Role = value.String
			}
		case selleruser.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field seller_users", values[i])
			} else if value.Valid {
				_m.seller_users = new(string)
				*_m.seller_users = value.String
			}
		case selleruser.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_seller_links", values[i])
			} else if value.Valid {
				_m.user_seller_links = new(string)
				*_m.user_seller_links = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SellerUser.
// This includes values selected through modifiers, order, etc.
func (_m *SellerUser) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QuerySeller queries the "seller" edge of the SellerUser entity.
func (_m *SellerUser) QuerySeller() *SellerQuery {
	return NewSellerUserClient(_m.config).QuerySeller(_m)
}

// QueryUser queries the "user" edge of the SellerUser entity.
func (_m *SellerUser) QueryUser() *UserQuery {
	return NewSellerUserClient(_m.config).QueryUser(_m)
}

// Update returns a builder for updating this SellerUser.
// Note that you need to call SellerUser.Unwrap() before calling this method if this SellerUser
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *SellerUser) Update() *SellerUserUpdateOne {
	return NewSellerUserClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the SellerUser entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *SellerUser) Unwrap() *SellerUser {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: SellerUser is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *SellerUser) String() string {
	var builder strings.Builder
	builder.WriteString("SellerUser(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("role=")
	builder.WriteString(_m.Role)
	builder.WriteByte(')')
	return builder.String()
}

// SellerUsers is a parsable slice of SellerUser.
type SellerUsers []*SellerUser
