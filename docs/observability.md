# Observability

Estruturada em logs JSON + correlacao por `request_id`/`correlation_id` + dashboards Kibana.

Cada servico deve registrar eventos HTTP e async com os campos obrigatorios da plataforma.
