apiVersion: 1

providers:
  - name: EntregaMais Dashboards
    orgId: 1
    folder: EntregaMais
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
